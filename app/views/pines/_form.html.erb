<%= form_with(model: pine, local: true) do |form| %>
  <% if pine.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(pine.errors.count, "error") %> prohibited this pine from being saved:</h2>

      <ul>
      <% pine.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nombre %>
    <%= form.text_field :nombre %>
  </div>

  <div class="field">
    <%= form.label :descripcin %>
    <%= form.text_field :descripcin %>
  </div>

  <div class="field">
    <%= form.label :foto %>
    <%= form.text_field :foto %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
